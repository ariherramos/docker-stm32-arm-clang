## Ambiente docker Compilação
### Compilando no ambiente Docker:
Entre na pasta

Coloque o comando all para criar o ambiente de compilação:

`. build_docker.sh all`

Para limpar os arquivos buildados:

`. build_docker.sh clean`

Apenas compilar:

`. build_docker.sh`
